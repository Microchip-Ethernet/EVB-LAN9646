diff -Napur linux-linux4microchip-2025.04-old/drivers/net/phy/phy_device.c linux-linux4microchip-2025.04-new/drivers/net/phy/phy_device.c
--- linux-linux4microchip-2025.04-old/drivers/net/phy/phy_device.c	2025-05-26 04:26:53.000000000 -0700
+++ linux-linux4microchip-2025.04-new/drivers/net/phy/phy_device.c	2026-04-27 18:49:04.598376528 -0700
@@ -384,8 +384,10 @@ static __maybe_unused int mdio_bus_phy_r
 	 * that something went wrong and we should most likely be using
 	 * MAC managed PM, but we are not.
 	 */
+#ifndef CONFIG_NET_DSA_MICROCHIP_KSZ_COMMON
 	WARN_ON(phydev->state != PHY_HALTED && phydev->state != PHY_READY &&
 		phydev->state != PHY_UP);
+#endif
 
 	ret = phy_init_hw(phydev);
 	if (ret < 0)
